# Dependencies
node_modules/

# Build outputs
dist/
build/
out/

# Electron
*.log

# macOS
.DS_Store

# Windows
Thumbs.db

# IDE
.vscode/
.idea/

# Environment
.env
.env.local

# Secrets / signing keys (NEVER commit)
*.p8
*.pem
*.p12
*.mobileprovision
AuthKey_*.p8

# Python (old POC)
__pycache__/
*.pyc
*.pyo

# User data (sensitive)
# Note: Keeping the history file for demo purposes
# In production, you might want to ignore this
# prompts-history.txt
release/

#Walkthroughs
WALKTHROUGH/*.p8
RELEASE.local.md

# Frame: per-machine task tracking. Frame rewrites this on every edit
# (and normalizes Unicode escapes) so diffs are constant cosmetic noise.
# Sharing the backlog with the team will go through tasks.shared.json
# once that lands — see lemo-6 in the backlog.
tasks.json
